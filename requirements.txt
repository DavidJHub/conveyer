# Core (required)
pandas>=2.0
numpy>=1.24
pyarrow>=12.0
scikit-learn>=1.3          # topics (KMeans/TF-IDF), the logistic conversion model
scipy>=1.10                # Jeffreys credible intervals
matplotlib>=3.7            # notebook figures
requests>=2.31             # online page fetching (offline/synthetic needs nothing)

# Optional upgrades — auto-detected at runtime, everything degrades gracefully
beautifulsoup4>=4.12       # better HTML parsing (stdlib html.parser fallback)
lxml>=5.0                  # fast parser backend for beautifulsoup4
# sentence-transformers>=2.6   # local embeddings for topics (TF-IDF fallback)
# bertopic>=0.16               # topic modelling (KMeans fallback)
# transformers>=4.40           # neural sentiment (lexicon fallback)
# voyageai>=0.3                # API embeddings (VOYAGE_API_KEY)
# anthropic>=0.40              # LLM-assisted page classification (ANTHROPIC_API_KEY)
