# Core (required)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
pyarrow>=12.0
scipy>=1.10
networkx>=3.0            # interaction network (conveyer.graphs)
matplotlib>=3.7          # notebook figures
requests>=2.31           # page scraping (conveyer.pages)
beautifulsoup4>=4.12     # HTML parsing (conveyer.pages)
lxml>=4.9                # fast bs4 backend

# Embeddings — pick at least one backend (auto-resolved, best first)
voyageai>=0.3            # voyage-3-large (Anthropic-recommended, best English embeddings)
# openai>=1.0            # text-embedding-3-large
sentence-transformers>=2.6   # open fallback (BAAI/bge-large-en-v1.5, Qwen3-Embedding, ...)

# Data-driven topic modelling (optional; KMeans works without these)
bertopic>=0.16
umap-learn>=0.5
hdbscan>=0.8

# LLM cluster naming / zero-shot intent / ClusterLLM-style granularity (optional)
anthropic>=0.40

# Visualization dashboard
plotly>=5.20

# ACES simulator (optional, for real VLM-agent runs — the offline logit agent
# in conveyer.aces needs nothing extra). Clone + install separately:
#   git clone https://github.com/mycustomai/ACES && cd ACES && uv sync --all-packages
